#! /bin/bash
case "${1:-ip}" in
	ip)
		curl -sfA "ipsb-wrapper/1.0" https://api.ip.sb/jsonip | jq
	;;
	whereisit|w)
		curl -sfA "ipsb-wrapper/1.0" "https://api.ip.sb/geoip/${2}" | jq
	;;
	help|*)
		echo 'usage: '
		echo '   ipsb ip / help / whereisit(w)+[ip address](default=my-ip)'
	;;
esac
